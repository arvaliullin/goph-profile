apiVersion: 1

providers:
  - name: goph-profile
    orgId: 1
    folder: GophProfile
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
